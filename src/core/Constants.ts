export enum Subject {
  GetTest,
  GetTests,
  SaveTest,
  ExistTest,
  GetTheme,
  SetTheme,
  SetBadge,
  GetVersion,
}

export enum Storage {
  Tests = 'tests',
  Theme = 'theme',
}

export enum Script {
  Background = 'background',
}
